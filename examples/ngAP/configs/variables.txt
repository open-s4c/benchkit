input_start_pos,
report_off,
split_entire_inputstream_to_chunk_size,
compress_prec_table,
data_buffer_fetch_size,
quit_degree,
result_capacity,
use_soa,
max_nfa_size,
use_uvm,
unique_frequency,
input_len,
precompute_cutoff,
precompute_depth,
algorithm,
group_num,
add_aas_interval,
remove_degree,
add_aan_start,
active_threshold,
duplicate_input_stream,
unique,
pc_use_uvm,
input,
isHS,
isVASim,
app_name,
quick_validation,
enable_validation,
output_file
